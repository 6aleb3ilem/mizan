export interface ClientExcel {
    id: number;
    name: string;
    telephone: string;
    email: string;
    address: string;
    status: string; // assuming status name is a string in the excel file
    note: string;
  }
  